package com.dale.talk.common

interface TalkConfig {
    companion object {
        const val userId = "userId"
        const val userName = "userName"
        const val passWord = "passWord"
        const val country = "country"
        const val token = "token"
    }
}
